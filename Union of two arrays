// a and b : given array with n and m size respectively

int doUnion(int a[], int n, int b[], int m)  {
    map<int,int> order;
    for(int i=0;i<n;i++){
        order[a[i]]++;
    }
    for(int i=0;i<m;i++){
        order[b[i]]++;
    }
    
    return order.size();
}
