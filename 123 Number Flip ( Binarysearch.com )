class Solution:
    def solve(self, n):
        t=str(n)
        if(t[0]!='3'):
            return int('3'+t[1:])
        else:
            for i in range(1,len(t)):
                if(t[i]=='1' or t[i]=='2'):
                    return int(t[0:i]+'3'+t[i+1:])
        
        return n


        
