class Solution:
    def solve(self, votes):
        temp=[0]*100002
        for i in range(0,len(votes)):
            if(temp[votes[i][1]]==0):
                temp[votes[i][1]]=1
            else:
                return True
        
        return False
