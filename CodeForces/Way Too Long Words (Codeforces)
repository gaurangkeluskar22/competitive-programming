n=int(input())
for i in range(n):
    ans=''
    string=str(input())
    if(len(string)>10):
        ans+=string[0]
        ans+=str(len(string[1:-1]))
        ans+=string[-1]
        print(ans)
    else:
        print(string)
 
