for _ in range(int(input())):
    def solve(p,q):
        if(p<=1000):
            return p*q
        else:
            total=p*q
            val=(total*10)//100
            return total-val
    p,q=map(int,input().strip().split())
    print(solve(p,q))
    
    
        
    
    
