int multiply(int arr[], int n)
{
    int sum0=0,sum1=0;
    for(int i=0;i<n;i++){
        if(i<=(n/2)-1){
            sum0+=arr[i];
        }
        else{
            sum1+=arr[i];
        }
    }
    return sum0*sum1;
}
