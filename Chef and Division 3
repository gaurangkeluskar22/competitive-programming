try:
    
    for i in range(int(input())):
        n,k,d=map(int,input().strip().split())
        arr=list(map(int,input().strip().split()))[:n]
        total=sum(arr)//k
        if(total<=d):
            print(total)
        else:
            print(d)

except:
    pass
