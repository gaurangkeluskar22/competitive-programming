for _ in range(int(input())):
    n=int(input())
    arr=list(map(int,input().strip().split()))[:n]
    arrsum=sum(arr)
    if(arrsum==n):
        print(0)
        
    elif(arrsum<n):
        print(1)
 
    else:
        print(arrsum-n)
