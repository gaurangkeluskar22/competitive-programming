class Solution:
    def countDigits(self, num: int) -> int:
        string=str(num)
        count=0
        for i in string:
            if(num%int(i)==0):
                count+=1
        return count
