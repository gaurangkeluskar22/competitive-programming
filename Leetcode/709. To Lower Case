class Solution:
    def toLowerCase(self, str: str) -> str:
        ans=""
        for i in str:
            if(ord(i)>=65 and ord(i)<=90):
                ans+=chr(int(ord(i))+32)
            else:
                ans+=i
        
        return ans
        
