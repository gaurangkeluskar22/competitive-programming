string reverseWord(string str){
    stack<char> s;
    string ans;
    for(int i=0;i<str.size();i++){
        s.push(str[i]);
    }
    int i=0;
    while(!s.empty()){
        ans+=s.top();
        s.pop();
        i+=1;
    }
    return ans;
}
