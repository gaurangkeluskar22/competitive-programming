class Solution:
    def primeSum(self, N):
        count=0
        prime=[2,3,5,7]
        while(N>0):
            rem=N%10
            if(rem in prime):
                count+=rem
            N=N//10
            
        return count
