class Solution:
    def mostWordsFound(self, sentences: List[str]) -> int:
        maxi=0
        for i in sentences:
            arr=list(map(str,i.strip().split()))
            if(len(arr)>maxi):
                maxi=len(arr)
        
        return maxi
        
