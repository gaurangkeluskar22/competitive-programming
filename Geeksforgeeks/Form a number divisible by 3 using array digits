class Solution:
    def isPossible(self, N, arr):
        s=''
        for i in arr:
            s+=str(i)
        s1=0
        for i in s:
            s1+=int(i)
        if(s1%3==0):
            return 1
        else:
            return 0
