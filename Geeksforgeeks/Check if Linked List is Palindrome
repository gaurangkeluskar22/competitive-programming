#Function to check whether the list is palindrome.
class Solution:
    def isPalindrome(self, head):
        #code here
        string=''
        while(head):
            string+=str(head.data)
            head=head.next
        
        if(string==string[::-1]):
            return 1
        else:
            return 0
