class Solution {
  public:
    string toLower(string S) {
        for(auto i=S.begin(); i!=S.end(); i++){
            if(*i>='A' && *i<='Z'){
            *i+=32;
            }
        }
        return S;
    }
};
