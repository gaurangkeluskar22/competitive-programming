int count(struct node* head, int search_for)
{
    int count=0;
    node* temp=head;
    while(temp!=NULL){
        if(temp->data==search_for){
            count+=1;
        }
        temp=temp->next;
    }
    return count;
}
