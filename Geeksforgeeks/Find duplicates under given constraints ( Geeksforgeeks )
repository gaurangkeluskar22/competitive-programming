#include <iostream>
using namespace std;
#include <bits/stdc++.h>
int main() {
	int t;
	cin>>t;
	while(t--){
	    map<int,int> m;
	    int temp=0;
	    for(int i=0;i<10;i++){
	        cin>>temp;
	        m[temp]++;
	    }
	    for(auto it:m){
	        if(it.second>1){
	            cout<<it.first<<endl;
	            break;
	        }
	    }
	}
	
	return 0;
}
