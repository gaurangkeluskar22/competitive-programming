class Solution:
    def lastIndex(self, s):
        index_of_one=-1
        
        for i in range(0,len(s)):
            if(s[i]=='1'):
                index_of_one=i
        
        if(index_of_one==-1):
            return -1
        else:
            return index_of_one
