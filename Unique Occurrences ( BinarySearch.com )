class Solution:
    def solve(self, nums):
        count=Counter(nums)
        ans=[]
        for i in count:
            ans.append(count[i])

        print(ans)
        s=set(ans)
        if(len(s)==len(ans)):
            return True
        else:
            return False
        
